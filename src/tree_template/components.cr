# Add to TreeTemplate::Components your differents components
# To add a component, just define a new method, and you're good to go!
# Each methods will be available to the template engine after that :)
module TreeTemplate::Components
  extend self
end
